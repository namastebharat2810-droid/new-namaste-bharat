This directory stores backend data for Namaste Bharat.

- `db.json` is created automatically on first API request.
- The file is seeded from `lib/backend/seed.ts`.
- Data persists across local restarts.
- `auth.json` stores local test auth users and sessions.
